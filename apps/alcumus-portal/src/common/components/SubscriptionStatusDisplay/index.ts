import { withTranslation } from 'react-i18next';
import { SubscriptionStatusDisplay } from './SubscriptionStatusDisplay';

export default withTranslation('subscriptionStatusDisplay')(
  SubscriptionStatusDisplay
);
