import SubscriptionContextMenu from './SubscriptionContextMenu';
import { withTranslation } from 'react-i18next';

export default withTranslation('subscriptionContextMenu')(
  SubscriptionContextMenu
);
